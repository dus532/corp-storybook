import styled from 'styled-components';

import IMG from 'images/icon_menu.png';

const IconMenu = styled.div`
  display: inline-block;
  width: 24px;
  height: 100%;
  background: url(${IMG}) center / contain no-repeat;
`;

export default IconMenu;
