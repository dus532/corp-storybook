import styled from 'styled-components';

import ErrorIMG from 'images/icon_error.png';

const IconError = styled.div`
  display: inline-block;
  width: 24px;
  height: 24px;
  background: url(${ErrorIMG}) center / contain no-repeat;
`;

export default IconError;
