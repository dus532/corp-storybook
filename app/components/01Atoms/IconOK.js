import styled from 'styled-components';

import OKIMG from 'images/icon_ok.png';

const IconError = styled.div`
  display: inline-block;
  width: 24px;
  height: 24px;
  background: url(${OKIMG}) center / contain no-repeat;
`;

export default IconError;
