import styled from 'styled-components';

import IMG from 'images/icon_confirm.png';

const IconConfirm = styled.div`
  display: inline-block;
  width: 40px;
  height: 40px;
  background: url(${IMG}) center / contain no-repeat;
`;

export default IconConfirm;
