import styled from 'styled-components';

import HideIMG from 'images/icon_hide.png';

const IconHide = styled.div`
  display: inline-block;
  width: 24px;
  height: 24px;
  background: url(${HideIMG}) center / contain no-repeat;
`;

export default IconHide;
