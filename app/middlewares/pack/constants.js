const KEY = {
  LIFECYCLE: 'redux-pack/LIFECYCLE',
  TRANSACTION: 'redux-pack/TRANSACTION',
};

const LIFECYCLE = {
  START: 'start',
  SUCCESS: 'success',
  FAILURE: 'failure',
};

export { KEY, LIFECYCLE };
