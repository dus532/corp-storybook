import styled from 'styled-components';

import IconIMG from 'images/icon_calender.png';

const IconCalender = styled.div`
  display: inline-block;
  width: 24px;
  height: 24px;
  background: url(${IconIMG}) center / contain no-repeat;
`;

export default IconCalender;
