import styled from 'styled-components';

import IConNoDataIMG from 'images/icon_card_black.png';

const IConNoData = styled.div`
  display: inline-block;
  width: 80px;
  height: 80px;
  background: url(${IConNoDataIMG}) center / contain no-repeat;
`;

export default IConNoData;
