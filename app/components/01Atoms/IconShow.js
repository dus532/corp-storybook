import styled from 'styled-components';

import ShowIMG from 'images/icon_show.png';

const IconShow = styled.div`
  display: inline-block;
  width: 24px;
  height: 24px;
  background: url(${ShowIMG}) center / contain no-repeat;
`;

export default IconShow;
