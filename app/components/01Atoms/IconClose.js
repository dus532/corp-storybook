import styled from 'styled-components';

import CloseIMG from 'images/icon_close.png';

const IconClose = styled.div`
  display: inline-block;
  width: 24px;
  height: 24px;
  background: url(${CloseIMG}) center / contain no-repeat;
`;

export default IconClose;
