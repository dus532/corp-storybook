import styled from 'styled-components';

import IMG from 'images/icon_status_ok.png';

const Icon = styled.div`
  display: inline-block;
  width: 20px;
  height: 20px;
  background: url(${IMG}) center / contain no-repeat;
`;

export default Icon;
